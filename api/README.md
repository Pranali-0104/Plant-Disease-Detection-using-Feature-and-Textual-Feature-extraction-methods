# API Placeholder

This package is reserved for Stage 2/3 FastAPI deployment.

Planned responsibilities:
- Load trained detector checkpoints.
- Expose single-image and batch inference endpoints.
- Return disease labels, confidence, bounding boxes, and future severity estimates.
- Support React and mobile clients without coupling API code to training scripts.
